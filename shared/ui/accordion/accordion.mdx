import { Meta, Canvas, Story, Controls } from "@storybook/addon-docs/blocks"
import * as AccordionStories from "./accordion.stories"

<Meta of={AccordionStories} />

# Accordion

O **Accordion** é um componente de interface utilizado dentro do design system para compor interfaces consistentes.

## Como funciona

Este componente encapsula estilos e comportamento reutilizável.

## Estrutura básica

```tsx
import { Accordion } from "@/components/ui/accordion"

export function Example() {
  return (
    <Accordion>
      Conteúdo
    </Accordion>
  )
}
```

## Exemplo básico

<Canvas>
  <Story of={AccordionStories.Default} />
</Canvas>
