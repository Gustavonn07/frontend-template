import { Meta, Canvas, Story, Controls } from "@storybook/addon-docs/blocks"
import * as CalendarStories from "./calendar.stories"

<Meta of={CalendarStories} />

# Calendar

O **Calendar** é um componente de interface utilizado dentro do design system para compor interfaces consistentes.

## Como funciona

Este componente encapsula estilos e comportamento reutilizável.

## Estrutura básica

```tsx
import { Calendar } from "@/components/ui/calendar"

export function Example() {
  return (
    <Calendar>
      Conteúdo
    </Calendar>
  )
}
```

## Exemplo básico

<Canvas>
  <Story of={CalendarStories.Default} />
</Canvas>
