import { Meta, Canvas, Story, Controls } from "@storybook/addon-docs/blocks"
import * as SonnerStories from "./sonner.stories"

<Meta of={SonnerStories} />

# Sonner

O **Sonner** é um componente de interface utilizado dentro do design system para compor interfaces consistentes.

## Como funciona

Este componente encapsula estilos e comportamento reutilizável.

## Estrutura básica

```tsx
import { Sonner } from "@/components/ui/sonner"

export function Example() {
  return (
    <Sonner>
      Conteúdo
    </Sonner>
  )
}
```

## Exemplo básico

<Canvas>
  <Story of={SonnerStories.Default} />
</Canvas>
